# ── Development only ──
# In production these are handled by the unified server (not needed).

# Backend API URL (only needed in development; in production the API is same-origin)
NEXT_PUBLIC_API_URL=http://localhost:3000

# Solana RPC (must match backend network — devnet for testing)
NEXT_PUBLIC_SOLANA_RPC_URL=https://api.devnet.solana.com

# Admin password (must match backend ADMIN_PASSWORD)
ADMIN_PASSWORD=
